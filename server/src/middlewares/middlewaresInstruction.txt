1.Naming Conventions

File Names: files should start with a lowercase letter, use camelCase, and end with a .middleware.js suffix. For example: user.middleware.js etc.File can contain multiple middleware function. U need to just export it.